apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-store
spec:
  provider:
    vault:
      server: 
        secretRef:
          name: vault-secret
          namespace: argocd
          key: vault_alb_url
      path: secret  # KV Engine 이름
      version: v2
      auth:
        tokenSecretRef:
          name: vault-secret
          namespace: argocd
          key: vault_token